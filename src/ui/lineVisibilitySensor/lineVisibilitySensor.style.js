import styled from 'styled-components';

const style = {};

style.Root = styled.div`
  position: relative;
  margin-top: -1px;
  height: 1px;
  z-index: -1;
`;

export { style };

export default style;
